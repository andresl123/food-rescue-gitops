apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: food-rescue-live
spec:
  type: ClusterIP
  selector:
    app: frontend # Must match the deployment's label
  ports:
    - protocol: TCP
      port: 8085 # Expose port 8085
      targetPort: 8085 # Target Nginx port 8085
