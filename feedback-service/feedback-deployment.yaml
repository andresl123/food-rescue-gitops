apiVersion: apps/v1
kind: Deployment
metadata:
  name: feedback-service-deployment
  namespace: food-rescue-live
spec:
  replicas: 1
  selector:
    matchLabels:
      app: feedback-service
  template:
    metadata:
      labels:
        app: feedback-service
    spec:
      securityContext:
        runAsUser: 1000
        runAsGroup: 3000
        fsGroup: 2000
        runAsNonRoot: true
      containers:
        - name: feedback-service
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          image: andresl123/feedback-service:feff0b0
          ports:
            - containerPort: 8084

          # Activate the K8s profile to load application-k8s.properties
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "k8s"

      nodeSelector:
        kubernetes.io/arch: arm64
